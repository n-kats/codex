# Legacy custom notes

This directory stores notes for custom features that are no longer part of the
current `custom2` feature set. Keep them here so the historical record stays
separate from the active custom notes under `_docs/custom_notes/`.

## Contents

- `additional_prompt_dirs/README.md`: legacy documentation for
  `CODEX_ADDITIONAL_PROMPT_DIRS`.
