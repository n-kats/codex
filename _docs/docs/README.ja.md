# README（日本語）

このファイルは、上流の `README.md` を日本語で参照したいときのための翻訳メモです。

- 原本: `README.md`
- 方針: 原本（非カスタム領域）は編集せず、`_docs/` 配下に日本語ドキュメントを追加する。

## クイックスタート

インストール例:

```shell
npm install -g @openai/codex
```

または:

```shell
brew install --cask codex
```

起動:

```shell
codex
```

関連ドキュメント（上流）:

- `docs/getting-started.md`
- `docs/install.md`
- `docs/faq.md`
- `docs/skills.md`

